const pool = require('../config/db');

class Meetup {
  static async getAll() {
    return [];
  }

  static async getById(id) {
    return null;
  }

  static async create(data) {
    return null;
  }
}

module.exports = Meetup;

